@Override
public boolean equals(Point another){
	//If these objects are same
	if(another == this)
		return true;
	
	//Check if another is an instance of Point
	if(!(another instanceof Point))
		return false;

	//Logic for equality
	


}